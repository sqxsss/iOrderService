spring:
  config:
    import: optional:configserver:http://localhost:8051
  datasource:
    url: jdbc:h2:mem:order
    username: admin
    password: 123456
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Oialect

eureka:
  client:
    enabled: false